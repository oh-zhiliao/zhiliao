services:
  agent:
    build:
      context: ./agent
      network: host
      args:
        USE_CN_MIRROR: ${USE_CN_MIRROR:-false}
        BASE_IMAGE: ${USE_CN_MIRROR:+m.daocloud.io/docker.io/library/node:22-slim}
    network_mode: host
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
      - ./plugins:/app/plugins
    environment:
      - USE_CN_MIRROR=${USE_CN_MIRROR:-false}
      - GIT_SSH_COMMAND=ssh -i /app/data/deploy_key -o UserKnownHostsFile=/app/data/ssh/known_hosts -o StrictHostKeyChecking=yes -o ConnectTimeout=10 -o ServerAliveInterval=15 -o ServerAliveCountMax=3
    depends_on:
      rag:
        condition: service_healthy

  rag:
    build:
      context: ./memo
      network: host
      args:
        USE_CN_MIRROR: ${USE_CN_MIRROR:-false}
        BASE_IMAGE: ${USE_CN_MIRROR:+m.daocloud.io/docker.io/library/python:3.12-slim}
    network_mode: host
    volumes:
      - ./data/memo:/app/data
      - ./data/repos:/app/repos:ro
      - ./config.yaml:/app/config.yaml:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8090/health')"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
